#include <memory_allocators_apis/kernel_allocator.h>

#include <gtest/gtest.h>
#include <random>
#include <ranges>

#include <format>


TEST(SlubAllocator, TestAllocate) {

  auto meme = kmalloc(10);

}

TEST(SlubAllocator, TestReallocate) {

}

TEST(SlubAllocator, TestFree) {

}